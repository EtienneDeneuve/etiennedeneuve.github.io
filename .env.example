# Copy to `.env` for local builds (never commit secrets).
# GitHub Actions: set repository secrets with the same names — wired in
# `.github/workflows/deploy.yml` and `pr.yml` as PUBLIC_* for Astro build-time inlining.

# Google Analytics 4 measurement ID (G-XXXXXXXX)
PUBLIC_GA4_MEASUREMENT_ID=G-DH1KQ6T6XX

# Microsoft Clarity project ID
PUBLIC_CLARITY_PROJECT_ID=xs4kgk0kth
