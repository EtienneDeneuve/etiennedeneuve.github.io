# Copy to `.env` for local builds (never commit secrets).
# GitHub Actions: set the same names as repository/environment variables.
# Until personal-site IDs are confirmed, site.ts falls back to provisional defaults.

# Google Analytics 4 measurement ID (G-XXXXXXXX)
PUBLIC_GA4_MEASUREMENT_ID=G-2Q8B9CW53L

# Microsoft Clarity project ID
PUBLIC_CLARITY_PROJECT_ID=itjvvvekvr
