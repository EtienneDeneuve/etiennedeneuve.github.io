<style is:inline is:global>
  /* Swiss Signal: flat paper/ink, no photo collage */
  .backgrounds {
    min-height: 100%;
    isolation: isolate;
    background-color: var(--color-bg);
  }

  @media (forced-colors: active) {
    .backgrounds {
      background: Canvas;
    }
  }
</style>
